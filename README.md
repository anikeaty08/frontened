# AquaReserve

AquaReserve is a privacy-preserving reserve-assurance project built for Midnight.

## Repository layout

- `frontend/` — the documentation and product website.
- `backend/` — the API, Compact contract, Midnight worker, tests, and deployment documentation.

Each application keeps its own package manifest and can be installed and run independently from its directory.
